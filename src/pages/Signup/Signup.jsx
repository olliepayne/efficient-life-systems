import SignupForm from '../../components/SignupForm/SignupForm'

const Signup = (props) => {
 // handle the submit of the signup data (through props, calls to the top level of our application)

 return (
  <div>
   <SignupForm />
  </div>
 )
}

export default Signup